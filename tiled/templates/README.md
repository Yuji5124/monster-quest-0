# tiled/templates/

> **legacy / reference**: このフォルダは既存Tiled方式向けの保管場所であり、新規マップ制作の標準ではない。新方式は [docs/MAP_SYSTEM.md](../../docs/MAP_SYSTEM.md) を参照する。

再利用するObjectテンプレート(`.tx`)を置く場所(将来用のプレースホルダ)。

- 2026-09-13時点で、このフォルダにテンプレートファイルは存在しない。
- 将来、spawn / exit / collision 等のObjectをTiled Templateとして共通化する場合にここへ追加する。
- 命名は [`tiled/extensions/mq0/config.mjs`](../extensions/mq0/config.mjs) の `naming` 設定と揃えること。
