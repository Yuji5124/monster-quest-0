import { SCALE_FACTOR } from "./display.ts";

/** DEV_PLACEHOLDER_NPC: Phase 7確認用の仮表示。正式NPC素材ではない。 */
export const NPC_VISUAL = {
  width: 12 * SCALE_FACTOR,
  height: 16 * SCALE_FACTOR,
  color: 0xd9a441,
} as const;
