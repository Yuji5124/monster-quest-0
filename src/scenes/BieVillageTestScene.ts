import { BieVillageScene } from "./BieVillageScene.ts";

/** DEV URL用。通常Sceneと同じ正式No.03画像マップを単独起動する。 */
export class BieVillageTestScene extends BieVillageScene {
  constructor() {
    super("BieVillageTestScene");
  }
}
