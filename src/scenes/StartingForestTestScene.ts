import { StartingForestScene } from "./StartingForestScene.ts";

/** DEV URL用。通常Sceneと同じ正式はじまりのもり画像マップを単体起動する。 */
export class StartingForestTestScene extends StartingForestScene {
  constructor() {
    super("StartingForestTestScene");
  }
}
