import { StartingPlaceScene } from "./StartingPlaceScene.ts";

/** DEV URL用。通常Sceneと同じ正式No.01画像マップを単体起動する。 */
export class ImageMapTestNo01Scene extends StartingPlaceScene {
  constructor() {
    super("ImageMapTestNo01Scene");
  }
}
